{
  "dependencies": {
    "angular": "^1.8.3",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.3.1",
    "ejs": "^4.0.1",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "mysql2": "^3.18.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "backend": "node index.js",
    "frontend:install": "npm install --prefix frontend",
    "frontend": "npm run frontend:install && npm start --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  }
}
